version: 2
issue-845-fix: true

packages:
  gin-demo/repository:
    config:
      dir: repository
      output: mocks
      outpkg: mocks
      recursive: true
      all: true

  gin-demo/services:
    config:
      dir: services
      output: mocks
      outpkg: mocks
      recursive: true
      all: true

